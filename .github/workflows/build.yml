name: build
on:
    push:
        branches: [main]

jobs:
    build_web:
        runs-on: ubuntu-latest
        steps: 
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                node-version: 22
            - name: try_npm
              run: npm -v